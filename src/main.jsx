import React from 'react'
import ReactDOM from 'react-dom/client'
import App from '../elevator_mgmt_system'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
)
